
def has_dependency_already_been_installed(hash: str) -> bool:
    has_been_installed = False
    return has_been_installed


def install_dependency_from_hash(hash: str):
    return


def get_dependency_hashes_from_project_names(project_names: list[str]) -> list[str]:
    dependency_hashes = []
    return dependency_hashes


def get_dependency_hashes_from_game_names(game_names: list[str]) -> list[str]:
    dependency_hashes = []
    return dependency_hashes


def get_dependency_hashes_dependency_configs(dependency_configs: list[str]) -> list[str]:
    dependency_hashes = []
    return dependency_hashes
